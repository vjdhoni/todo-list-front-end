export const Loading = () => {
    return (
        <div style={{ textAlign: 'center' }}>
            <i className={`fa fa-gear fa-spin`} style={{ fontSize: '64px', color: 'grey' }} />
        </div>
    )
}